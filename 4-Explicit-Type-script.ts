// UNION (of different types)
let a: (number | string)[] = [1, 2];

a.push('Risal');

// Object
let c: object;
/* c = {
    x: 1,
    y: 2
} */

c = [1,2,3];    // In JS, Array is Object as well! so, no error here